{
  "name": "R2 Publishing Workflow",
  "image": "mcr.microsoft.com/devcontainers/python:3.12",
  "features": {
    "ghcr.io/rocker-org/devcontainer-features/quarto-cli:1": { "version": "1.9.38", "installTinyTeX": true }
  },
  "postCreateCommand": "pip install -r requirements-dev.txt && sudo apt-get update && sudo apt-get install -y fonts-crosextra-carlito libreoffice-writer emf2svg libwmf-bin librsvg2-bin poppler-utils && sudo fc-cache -f && bash engine/scripts/install_tex.sh",
  "customizations": {
    "vscode": {
      "extensions": ["quarto.quarto", "ms-python.python"]
    }
  }
}
